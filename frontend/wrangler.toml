name = "agants"
pages_build_output_dir = "."
compatibility_date = "2024-09-23"
# AGANTS_BACKEND and AGANTS_ADMIN are injected by deploy.sh --pages via [vars]
# at deploy time. Do not commit a [vars] block — the tunnel URL changes on each
# cloudflared restart and must not be stored in version control.
