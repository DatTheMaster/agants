name = "agants"
pages_build_output_dir = "."
compatibility_date = "2024-09-23"
# AGANTS_BACKEND and AGANTS_ADMIN are set as environment variables in the
# Cloudflare Pages dashboard (or via CF API). The functions/_middleware.js
# serves them dynamically at /config.js — no redeploy needed when the
# tunnel URL changes, just update the env var via the CF dashboard or API.
