{
  "name": "agants-frontend",
  "private": true,
  "scripts": {
    "deploy": "wrangler pages deploy . --project-name agants --branch main --commit-dirty=true",
    "deploy:preview": "wrangler pages deploy . --project-name agants"
  },
  "devDependencies": {
    "wrangler": "^3"
  }
}
